version: '3.8'

services:
  scrabble-checker:
    image: ghcr.io/landcraft/sistermi-scrabble-checker:latest
    container_name: sistermi-scrabble-checker
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
